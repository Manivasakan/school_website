# React 19 RC ships with Next 15.0.x. Several deps (Tiptap, qrcode.react, etc.)
# declare `^19.0.0` as their peer dep but npm treats RC versions strictly, so
# the check fails. The packages work fine at runtime — bypass the check here.
legacy-peer-deps=true
